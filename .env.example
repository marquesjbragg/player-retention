# Copy this file to .env.local and fill in the values.
# .env.local is gitignored and must never be committed.

# Required for D1 CBBD provider (server-side only — never pass to client)
CBB_DATA_API_KEY=
