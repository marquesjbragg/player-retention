// Phase 2: Define the provider interface.
// All D1 and D2 providers must implement this contract.
// IDataProvider is the only thing the RetentionEngine and BrowseRow builder should import from providers.

export {};
