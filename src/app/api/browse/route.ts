// Phase 6: GET /api/browse
// Returns BrowseRow[] for the requested year pair and optional filters.
// Reads from TeamRegistry + D1CBBDProviders + RetentionEngine.

export async function GET() {
  return Response.json({ message: "not yet implemented" }, { status: 501 });
}
