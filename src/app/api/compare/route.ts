// Phase 8: GET /api/compare?team={slug}&year1={year}&year2={year}
// Returns full RetentionResult + stat deltas + ratings context for a single team year pair.

export async function GET() {
  return Response.json({ message: "not yet implemented" }, { status: 501 });
}
