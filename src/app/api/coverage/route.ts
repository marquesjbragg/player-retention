// Phase 6+: GET /api/coverage
// Returns data coverage summary — which teams have full data, stats-only, or missing data.

export async function GET() {
  return Response.json({ message: "not yet implemented" }, { status: 501 });
}
